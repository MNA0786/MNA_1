# 🎬 Entertainment Tadka Telegram Bot

A multi-channel movie search and forwarding Telegram bot.

## 📋 Features
- Multi-channel support (3 public channels + 3 private channels)
- CSV database with locked format: `movie_name,message_id,channel_id`
- Typing indicators
- Request group auto-handling
- Environment-based configuration
- Docker support

## 🚀 Quick Start

1. **Clone repository**
2. **Copy `.env.example` to `.env` and configure:**
   ```bash
   cp .env.example .env
   nano .env
